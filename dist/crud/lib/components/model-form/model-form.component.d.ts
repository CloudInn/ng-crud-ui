import { OnChanges, EventEmitter } from '@angular/core';
import { FormGroup, FormArray } from '@angular/forms';
import { ApiService } from '../../services/api.service';
import { Registry } from '../../services/registry.service';
import { Model } from '../../screens';
import { FormService } from '../../services/form.service';
import { FieldType, Field, AutoCompleteField } from '../../forms';
export declare class ModelFormComponent implements OnChanges {
    private api;
    private reg;
    private formService;
    moduleName: string;
    appName: string;
    modelName: string;
    mode: string;
    id: number;
    ngModel: any;
    model: Model;
    fieldType: typeof FieldType;
    AutoCompleteField: typeof AutoCompleteField;
    fields: Field[];
    choices: {};
    submit: EventEmitter<any>;
    form: FormGroup;
    formset: FormArray;
    formsets: FormArray[];
    is_ready: boolean;
    constructor(api: ApiService, reg: Registry, formService: FormService);
    ngOnChanges(): void;
    _onSubmit(): void;
    buildForm(values: any): void;
}
